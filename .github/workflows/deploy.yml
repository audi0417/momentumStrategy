name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Setup Pages
        uses: actions/configure-pages@v4
      
      - name: List files for debugging
        run: |
          echo "=== Root directory files ==="
          ls -la
          echo "=== Web directory files ==="
          ls -la web/
          echo "=== Checking specific files ==="
          test -f historical_data.json && echo "✅ historical_data.json exists" || echo "❌ historical_data.json missing"
          test -f web/index.html && echo "✅ web/index.html exists" || echo "❌ web/index.html missing"
          test -f web/historical_data.json && echo "✅ web/historical_data.json exists" || echo "❌ web/historical_data.json missing"
          test -f web/stock_price_data.json && echo "✅ web/stock_price_data.json exists" || echo "❌ web/stock_price_data.json missing"

      - name: Prepare GitHub Pages files
        run: |
          # Copy web files to root for GitHub Pages
          cp web/index.html .
          cp web/style.css .
          cp web/script.js .
          cp web/stock_price_data.json .
          cp web/historical_data.json .
          echo "=== Files after copying ==="
          ls -la *.html *.css *.js *.json
      
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'
      
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4